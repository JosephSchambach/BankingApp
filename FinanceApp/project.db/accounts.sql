CREATE TABLE accounts (id INTEGER PRIMARY KEY AUTOINCREMENT, user_id INTEGER NOT NULL, account TEXT NOT NULL, amount NUMERIC NOT NULL, type TEXT NOT NULL, descrip TEXT NOT NULL, time TIMESTAMP DEFAULT CURRENT_TIMESTAMP, name INTEGER, FOREIGN KEY(user_id) REFERENCES users(id));